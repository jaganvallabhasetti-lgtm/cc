class Solution:
    def checkPangram(self,s):
        #code here
        res=set()
        s=s.lower()
        for i in s:
            if i.isalpha():
                res.add(i)
        if len(res)==26:
            return True
        else :
            return False
        
