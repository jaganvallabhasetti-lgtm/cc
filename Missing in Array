class Solution:
    def missingNum(self, arr):
       n=len(arr)+1
       store=(n*(n+1))//2
       result=0
       for i in range(0,len(arr),1):
           result+=arr[i]
       miss=store-result
       return miss
