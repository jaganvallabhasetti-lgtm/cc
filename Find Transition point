class Solution:
    def transitionPoint(self, arr): 
        # Code here
        result=''
        a=arr[0]
        if a==0:
         result=-1
        else:
         result=0
        for i in range(0,len(arr)-1):
            if arr[i]!=arr[i+1]:
                result=i+1
                break
        return result
